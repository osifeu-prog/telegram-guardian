# Whitelist rules (committed defaults)
# keys:
# peer_type:peer_id   reason
# peer_id             reason
# title_regex:<rx>    reason
#
# Examples:
# title_regex:(?i)\b(engineering|family|work)\b  safe titles
